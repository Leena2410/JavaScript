let age = 30;
age = 31; 
// Reassigning a value (Mutate)

const birthYear = 2003; // cannot be reassignned (immutable variable)
//birthYear = 2004;  // ERROR

var job = "job no.1";
job = "job no.2";

lastName = "name";
console.log(lastName);
